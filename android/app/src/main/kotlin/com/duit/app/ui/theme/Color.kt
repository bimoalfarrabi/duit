package com.duit.app.ui.theme

import androidx.compose.ui.graphics.Color

// DESIGN.md semantic tokens
val Primary = Color(0xFF10B981)       // emerald-500 — income, CTA
val PrimaryDark = Color(0xFF059669)   // emerald-600 — hover
val PrimaryLight = Color(0xFFD1FAE5)  // emerald-100 — income chip bg
val Danger = Color(0xFFEF4444)        // red-500 — expense, error
val DangerLight = Color(0xFFFEE2E2)   // red-100 — expense chip bg
val Surface = Color(0xFFFFFFFF)       // card background
val Background = Color(0xFFF8FAFC)    // slate-50
val Border = Color(0xFFE2E8F0)        // slate-200
val TextPrimary = Color(0xFF0F172A)   // slate-900
val TextSecondary = Color(0xFF64748B) // slate-500
val TextMuted = Color(0xFF94A3B8)     // slate-400
